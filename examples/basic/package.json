{
  "name": "basic-example",
  "version": "0.0.1",
  "description": "A basic example of a TypeScript library that is compiled by Package Bundler. Clone this repository, cd to this folder, run npm install and then npm run build. The built contents should end up in ./dist by default.",
  "files": ["dist/**"],
  "main": "dist/index.cjs.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/ && package-bundler"
  },
  "dependencies": {
    "randomatic": "^3.1.1"
  },
  "devDependencies": {
    "@better-builds/package-bundler": "^1.2.5",
    "@types/randomatic": "^3.1.3",
    "typescript": "^4.6.3"
  }
}
